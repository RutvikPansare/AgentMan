# Tellero - Done

<!--
This is the WORK layer: an append-only archive of completed tasks.
When a task on docs/board.md is finished, cut its line here under today's date.
Newest date first. Don't edit history - only append.
-->

## 2026-06-22
- [x] **T-134** Story Replies epic - PARENT complete (T-149 through T-154 all shipped). Auto-DM when a follower replies to a story: settings + interaction-dedup schema, webhook detection (separate from regular DMs), the keyword-filter + send engine, GET/PUT settings API, the settings UI, and this self-review. Last remaining M12 Instagram epic: T-137 (DM Keywords), flagged "break into sub-tasks before starting."
- [x] **T-154** Story Replies: self-review (M12). Verification-only pass over T-149-T-153, all 10 checklist items confirmed: both migrations correct (schema, RLS via `org_members`, `UNIQUE(org_id)` on settings + `UNIQUE(org_id, story_id, instagram_user_id)` on interactions); story-reply detection sits before the regular-DM path in the webhook `messaging` loop and `continue`s, so regular DMs (no `story_mention` attachment) flow through untouched; keyword filter skips non-matching replies when `keyword_filter` is set and is bypassed entirely when empty; dedup keys on (org_id, story_id, instagram_user_id) so a repeat reply to the *same* story is skipped but a reply to a *different* story produces a new dedup query → new DM; DM variable substitution (`{{first_name}}`/`{{discount_code}}`/`{{brand_name}}`) and unique-per-user Shopify discount (`generateDiscountCode` + `createDiscountCode`) both covered; every settings route resolves `org_id` server-side; settings UI is scrollable with Save in the header. 74 instagram tests + full suite green, tsc clean. No bugs found - no code changes. Also removed a stale duplicate T-148 entry still sitting in `todo.md` (the real T-148 shipped earlier as `9aff96f`). One benign edge note (not fixed, not a bug): if `enabled` is true but `dm_message` is empty, an interaction row is still written with `dm_sent_at = null`, which would dedup a later reply with no DM having been sent - harmless since there's nothing to send without a configured message.
- [x] **T-153** Story Replies: settings UI (M12). Filled in `/dashboard/instagram/story-replies` (stub from T-136): single-page settings (order-notifications pattern, not the broadcast list), with Save in the page `Header`'s `actions` slot (settings/brand pattern) rather than order-notifications' inline-per-card save - the whole settings object is loaded once and saved as one `PUT`. Sections in order: stats card (`StoryReplyStatsCard` - total story replies triggered, DMs sent, via a new `useStoryReplyStats` hook querying `story_reply_interactions` counts client-side); `StoryReplySettingsCard` with enabled toggle, keyword-filter chip input (same helper copy as spec), DM message editor (chip buttons insert `{{first_name}}`/`{{discount_code}}` (only when discount is on)/`{{brand_name}}` at the cursor), discount toggle + percent input mirroring the winback/reorder inline pattern (no extractable shared component exists for that section - confirmed by grep, so mirrored the pattern rather than inventing a new shared one). Page is a flex column with a scrollable content region (`flex:1, minHeight:0, overflowY:auto`) under the fixed header, same fix as T-148 applied from the start this time. UI-only, no TDD per spec. tsc clean; visual check via dev server logs confirmed it compiles with zero console errors (same auth-redirect constraint as every other page this session, so no logged-in screenshot). Full suite 395/395 (no new tests - UI only). Next: T-154 self-review.
- [x] **T-152** Story Replies: API routes (M12). `GET /api/instagram/story-reply-settings` returns the org's settings row, creating a default one (`insert({org_id})`, all other columns fall back to their DB defaults) on first call so the settings UI always has something to render. `PUT` upserts via `onConflict: 'org_id'` against an allowlisted field set, validating `discount_percent` is 1-100 whenever `discount_enabled` is true (rejects both out-of-range and missing values; `discount_enabled: false` requires no percent). Both routes resolve `org_id` server-side via `getOrgId`. TDD: 7 tests (auth, org-scoped read, default-row creation, validation rejects, upsert payload shape, discount-off bypass). Full suite 395/395, tsc clean. Next: T-153 settings UI.
- [x] **T-151** Story Replies: automation engine (M12). Filled in `handleStoryReply` in `src/lib/instagram/story-replies.ts`: loads the org's single `instagram_story_reply_settings` row (exits if not enabled) → optional keyword filter via `matchesKeywords` (skipped entirely when no filter is set, not just "matches everything") → dedup via `story_reply_interactions` UNIQUE(org_id, story_id, instagram_user_id) → optional Shopify discount (`generateDiscountCode` + `getShopifyClientForOrg`) → DM via `InstagramAdapter.sendText` with `{{first_name}}`/`{{discount_code}}`/`{{brand_name}}` substitution → records the interaction. Whole function wrapped in try/catch so a webhook-triggered failure never throws. **Same spec deviation as T-142, applied without re-asking**: sends via `InstagramAdapter.sendText` (the adapter's real method), not the spec's `sendMessage`. TDD: 5 new engine tests (not-enabled exit, keyword-filter no-match exit, filter skipped when unset, dedup skip, full match path with discount + substituted DM + interaction row) + 1 never-throws test; `matchesKeywords` mocked per spec, not re-tested. 11/11 in this file, full suite 388/388, tsc clean. Next: T-152 API routes.
- [x] **T-150** Story Replies: webhook detection (M12). New `src/lib/instagram/story-replies.ts` with `isStoryReply(messaging)` (checks `message.attachments[0].type === 'story_mention'`), `extractStoryReply(messaging)` (pulls `story_id` from `attachments[0].payload.id`, `from_id` from `sender.id`, `text` from `message.text`; returns null if any are missing), and a `handleStoryReply` stub (logs only - T-151 fills it in). Wired into the Instagram `messaging` loop in `webhooks/meta/route.ts`: story replies are detected and routed before the existing regular-DM guard, then `continue` so they never fall through to conversation/message creation or the bot trigger. TDD: 6 unit tests (detection true/false/missing-fields, field extraction success/null, stub doesn't throw) + 2 new route-level tests confirming a story-reply DM calls `handleStoryReply` and skips `findOrCreateConversation`/message insert entirely, while a regular DM does the opposite. Full suite 383/383, tsc clean. Next: T-151 automation engine.
- [x] **T-149** Story Replies: DB schema (M12). Two additive migrations, write-only per spec: `20260622_05_instagram_story_reply_settings.sql` (one row per org - `enabled`, `keyword_filter text[]`, `dm_message`, `discount_enabled`/`discount_percent`, `unique(org_id)`) and `20260622_06_story_reply_interactions.sql` (per-replier dedup/attribution - `story_id`, `instagram_user_id`, `discount_code`, `order_id` FK→orders for future Shopify attribution, `dm_sent_at`, `unique(org_id, story_id, instagram_user_id)`). Both RLS-scoped via `org_members`, same pattern as `instagram_comment_campaigns`. **Applied the T-145 precedent up front**: used `dm_message text` instead of the spec's `dm_template_id` FK into the WhatsApp `templates` table - that table is shaped around Meta's approval workflow which free-text Instagram DMs don't go through (see decision log under T-145). Rutvik reviewed the SQL and will apply via the Supabase dashboard SQL editor himself. tsc clean. Next: T-150 webhook detection.
- [x] **T-148** Comments epic: self-review (M12). **Bug fix 1**: `CampaignEditor` was an unbounded flex column inside a fixed-height, `overflow-hidden` dashboard shell - content past the viewport was clipped, not scrollable. Restructured into a flex column with a scrollable content region (`flex:1, minHeight:0, overflowY:auto`) and a sticky save/cancel footer pinned outside the scroll area; `/new` and `/[id]` pages updated to pass through the available height. **Bug fix 2**: moved "New campaign" from a standalone button above the list into the page `Header`'s `actions` slot (same pattern as `settings/brand`/`profile`/`team`), converting the list page to a client component for the `Link`; removed the old button, kept the contextual one in the empty state. **Full review checklist** - all 13 items verified: 3 migrations re-read (schema/RLS/UNIQUE constraints all correct, RLS scoped via `org_members` directly on campaigns and via a `campaign_id` join on the two child tables); confirmed `subscribeToWebhooks` requests `subscribed_fields: "messages,comments"` on every Instagram connect; keyword edge cases, dedup, public reply skip-when-empty, DM substitution, and unique-discount-per-commenter are all already TDD-covered from T-142; confirmed `campaign_interactions` inserts correctly omit `order_id` (nullable, defaults null - future Shopify order attribution, not wired yet by design); re-verified every comment-campaign route filters by `org_id` server-side (GET list, GET/PUT/DELETE `[id]`, media); media picker cursor-pagination and multi-select-persists-on-save confirmed via existing API tests + the editor's two-step save. Full suite 375/375, tsc clean. No new tests needed - this was a verification + 2 UI fixes pass, not new logic.
- [x] **T-133** Comments epic - PARENT complete (T-140 through T-145 all shipped). Keyword-triggered Instagram DM automation: campaign config + target media + interaction-dedup schema, webhook subscription/parsing, the keyword-match + send engine, full CRUD + media-picker API, and the list + editor UI. Next Instagram epic: T-134 (Story Replies) or T-137 (DM Keywords), both flagged "break into sub-tasks before starting."
- [x] **T-145** Comments: campaign editor UI (M12). New `CampaignEditor` shared by `/dashboard/instagram/comments/new` (create) and `/dashboard/instagram/comments/[id]` (edit, new route - also wired T-144's cards to navigate here on click). Six sections per spec: name input; keyword chip input (add on Enter/comma, remove via x, min 1 required - save disabled until satisfied); post-scope toggle with a thumbnail media grid (checkbox-style multi-select + Load more, fetched from `GET /api/instagram/media`) when scope is "specific"; public reply textarea (1000-char limit, live counter, "Visible to everyone" helper); DM message editor (chip buttons insert `{{first_name}}`/`{{discount_code}}` (only shown when discount is on)/`{{post_url}}`/`{{brand_name}}` at the cursor into a plain textarea); discount toggle + percent input mirroring winback/reorder's inline pattern. Save POSTs (create) or PUTs (edit); on create, target media is attached in a second PUT once the campaign id exists. **Design deviation, confirmed with Rutvik**: the spec's `dm_template_id` FK into the WhatsApp `templates` table didn't fit - that table is shaped around Meta's template-approval workflow (status/category/language/components JSON) and Instagram DMs are free-text with no approval step. Added `dm_message text` directly on `instagram_comment_campaigns` (additive migration `20260622_04_comment_campaigns_dm_message.sql`) instead; updated the T-142 engine to read `dm_message` directly (dropped the `templates` table lookup entirely) and the T-143 CRUD routes' field lists to match. Also extended `PUT .../[id]` to accept an optional `target_media` array (full delete-then-insert replace) since T-143 had no write path for it and the post-scope picker needs one. TDD for all logic changes: engine tests updated for the new field, 1 new route test for the `target_media` replace path (8 instagram route/lib test files, 39/39 passing). Editor UI itself is UI-only, no TDD per spec. tsc clean; visual check blocked by auth (redirects to `/login`, no credentials this session) - confirmed via dev server logs/screenshot that it compiles and redirects correctly with zero console errors. Full suite 375/375.
- [x] **T-144** Comments: campaign list UI (M12). Filled in `/dashboard/instagram/comments` (stub from T-136), wrapped in `InstagramGate`. New `CommentCampaignList` client component: fetches `GET /api/instagram/comment-campaigns`, renders a card grid (name, keyword chips, enabled/disabled toggle wired to `PUT .../[id]`, interaction count + DMs sent stat), empty state ("Create your first Comment campaign" + button), loading/error states. Card grid + toggle markup follow the existing broadcast list and automation-settings toggle patterns for visual consistency. "New campaign" routes to `/dashboard/instagram/comments/new` - added as a stub page (mirrors the T-136 stub pattern) since the full editor is T-145. **Extended T-143's list API** to also return `dm_sent_count` per campaign (count of interactions with `dm_sent_at` set) - the spec needed both interaction count and DM count as separate stats, which the original endpoint didn't expose; added a test case for it (1 new test, route file now 5/5). UI-only, no TDD on the page itself per spec. tsc clean; visual check blocked by auth (dashboard redirects to `/login`, no credentials this session) - confirmed via dev server logs that it compiles with no errors/warnings beyond benign Fast-Refresh noise. Full suite 374/374. Next: T-145 campaign editor UI.
- [x] **T-143** Comments: CRUD API + media picker API (M12). `GET/POST /api/instagram/comment-campaigns` (list with `interaction_count` per campaign, create with validated `name`). `GET/PUT/DELETE /api/instagram/comment-campaigns/[id]` (single campaign + its `target_media` list, update via an allowlisted field set, delete cascades `campaign_interactions` → `campaign_target_media` → the campaign itself explicitly rather than relying solely on the DB's `ON DELETE CASCADE`). `GET /api/instagram/media` - calls Graph API `GET /{ig-user-id}/media` using the org's stored `instagram_connections.access_token`, cursor-paginated via `?after=`, 422 if Instagram isn't connected, 502 on a Graph API error. Every route resolves `org_id` server-side via `getOrgId` and scopes/verifies ownership before reading or mutating - no client-supplied org_id is ever trusted. TDD: 13 tests across the three route files (auth, org scoping, validation, 403 on cross-org access, cascade-order delete, Graph API success/cursor-forwarding/error paths). Full suite 374/374, tsc clean. Next: T-144 campaign list UI.
- [x] **T-142** Comments: keyword matching engine + campaign automation engine (M12). New pure utils: `src/lib/instagram/matchesKeywords.ts` (whole-word, case-insensitive, OR logic via tokenization - strips punctuation/emojis so "sale!" and "sale🔥" match but "wholesale" does not; multi-word keywords matched as a contiguous run) and `src/lib/instagram/generateDiscountCode.ts` (shared `PREFIX-XXXXXX` generator, mirrors the inline winback/reorder/lead-capture pattern). Filled in `src/lib/instagram/comments.ts` `handleInstagramComment(orgId, payload)`: loads the IG connection + enabled campaigns, then per campaign runs scope check (all-posts, or `campaign_target_media` must include the comment's media) → keyword match → idempotency (`campaign_interactions` dedup on campaign+commenter) → public reply → discount (Shopify `createDiscountCode` via `getShopifyClientForOrg`) → templated private DM via `InstagramAdapter.sendText` with `{{first_name}}`/`{{discount_code}}`/`{{post_url}}`/`{{brand_name}}` substitution → records the interaction. Fire-and-forget safe: per-campaign try/catch, external-call failures logged not thrown. **Two spec deviations**: (1) public reply posts to `POST /{comment_id}/replies` (the actual Instagram Graph endpoint for replying to a comment) rather than the spec's `/{media-id}/replies` which is not a valid endpoint; (2) the DM sends via `InstagramAdapter.sendText` (the adapter's actual method) not the spec's `sendMessage`. TDD: 7 `matchesKeywords` edge-case tests, 3 `generateDiscountCode` tests, 5 engine tests (no-campaigns, no keyword match, idempotency skip, full match path with public reply + discount + substituted DM + interaction row, specific-scope media miss). Full suite 361/361, tsc clean. Next: T-143 CRUD + media picker API.
- [x] **T-141** Comments: webhook subscription + parsing. Extended `connectInstagramAccount` to subscribe the app to `messages,comments` fields via `/{ig-user-id}/subscribed_apps` POST. Extended `src/app/api/webhooks/meta/route.ts` to detect `field === 'comments'`, extract comment metadata, and route to a new `handleInstagramComment` stub in `src/lib/instagram/comments.ts`. Added tests in `src/app/api/webhooks/meta/route.test.ts` and `src/lib/instagram/connect.test.ts`.
- [x] **T-140** Comments: DB schema (M12). Three additive migrations, write-only per spec (no logic this task): `20260622_01_instagram_comment_campaigns.sql` (campaign config: name, enabled, post_scope, keywords text[], public_reply_text, dm_template_id FK→templates, discount_enabled/percent), `20260622_02_campaign_target_media.sql` (specific posts/reels a campaign targets when post_scope='specific', unique(campaign_id, media_id)), `20260622_03_campaign_interactions.sql` (per-commenter attribution + dedup, unique(campaign_id, instagram_user_id), order_id FK→orders for future Shopify attribution). All RLS-scoped via `org_members` (campaigns directly by org_id, the other two via a campaign_id join) - same pattern as `instagram_connections`. Rutvik reviewed the SQL and will apply via the Supabase dashboard SQL editor himself (not run by Claude). Next: T-141 webhook subscription + parsing.
- [x] **T-146** Bypass `InstagramGate` on Instagram sub-pages (M12). Added a `GATE_ENABLED = false` module-level flag in `src/components/instagram/InstagramGate.tsx` - when false, all Instagram sub-pages (Comments, Story Replies, DM Keywords, Bot settings) render `children` immediately, skipping the `GET /api/settings/instagram` fetch and the loading/not-connected states. Gating logic untouched, not deleted - flipping the flag back to `true` (T-147, before public launch) fully reinstates it. Placed the early-return after all hooks (the `useEffect` now early-returns internally instead) to avoid a rules-of-hooks violation. UI-only, no TDD per spec; tsc clean.
- [x] **T-138** Resolve real Instagram usernames for inbound DMs. New `src/app/api/webhooks/meta/instagramProfile.ts` - `fetchInstagramSenderName(psid, accessToken)`, pure-HTTP testable (mirrors `embeddedSignup.ts`), calls Graph API `GET /{psid}?fields=name`, never throws (try/catch, logs, returns null on any failure or missing token). Wired into the Instagram webhook branch: selects `access_token` alongside `org_id`/`bot_enabled` from `instagram_connections`, checks for an existing conversation's `customer_name` first and only calls the Graph API when absent (avoids repeat calls on every message from the same PSID), passes the resolved name into `findOrCreateConversation`'s existing `customerName` param, and backfills the name on a pre-existing nameless conversation row. **Deviated from the written spec's "write to `contacts.name`"**: `contacts` is WhatsApp-only by design (phone `NOT NULL`, used by segments/broadcast targeting) - confirmed with Rutvik and wrote to `conversations.customer_name` instead, which is already how the inbox UI reads names; skipped `profile_pic` too since no UI surface renders it. TDD: 4 unit tests on `fetchInstagramSenderName` + 3 new route-level tests (name resolved + written on first message, Graph API skipped when name already known, message still saved when the Graph call throws). Full suite 345/345 pass, tsc clean.
- [x] **T-132** Instagram inbox polish (verification + fix). Traced the full path: registry returns `InstagramAdapter` for `'instagram'` (confirmed wired since T-128), `ChannelIcon` already renders the IG glyph (T-125), reply route is channel-agnostic and sends via the adapter correctly. **Found and fixed two real gaps**: (1) `ConversationThread.tsx` fell back to the WhatsApp-only `TemplateReplyBox` (template send + `NewConversationModal`, which posts to the WhatsApp-template-only `/api/inbox/new` route) whenever the 24h window closed, with no channel check - this would have rendered a broken WhatsApp flow against an Instagram PSID. Added `InstagramWindowClosedNotice.tsx` and branch on `conversation.channel` so Instagram threads outside the window show a simple "wait for the customer to message again" notice instead (Instagram has no re-engagement template equivalent). (2) `useReply.ts`'s optimistic message hardcoded `channel: 'whatsapp'` (a leftover tsc-satisfying placeholder from T-121) - now takes the conversation's actual channel as a param, threaded from `ConversationThread`. Confirmed this had zero visible impact (no component reads `message.channel`) but fixed it since the data was simply wrong. **Logged as new backlog task** (not built - out of polish scope): T-138, Instagram threads show the raw PSID as the customer's name since the webhook never resolves the real IG username via Graph API. UI-only fix, no TDD per spec; full suite 338/338 pass, tsc clean.
- [x] **T-131** Instagram bot settings page. **Migration** `20260622_instagram_bot_enabled.sql` (additive): `instagram_connections.bot_enabled boolean NOT NULL DEFAULT false` - needs manual application via Supabase dashboard SQL editor, flagged to Rutvik. **`PATCH /api/settings/instagram/bot`**: `{ enabled: boolean }` updates `bot_enabled` for the org's connection; 401 unauthenticated, 400 non-boolean, 500 on db error. **`GET /api/settings/instagram`** extended to also return `botEnabled`. **Webhook** (`webhooks/meta/route.ts` Instagram branch): now selects `bot_enabled` alongside `org_id` on the `instagram_connections` lookup, and after saving the inbound message + bumping unread count, fires the existing channel-agnostic `triggerBot` (same helper WhatsApp uses, already takes a `channel` param) only when `bot_enabled` is true - bot stays silent for orgs that haven't opted in. **UI**: filled in the `/dashboard/instagram/bot` stub with a single settings card (toggle mirrors the existing `Toggle` pattern from `WinbackSettingsCard`), optimistic update with rollback + inline error on PATCH failure. TDD: 4 new tests on the PATCH route (auth/validation/success/db-error), 1 new test on the webhook route (bot triggered with correct args when `bot_enabled` true) plus the existing Instagram webhook test extended to assert the bot is *not* triggered when the flag is absent/false. Full suite 338/338 pass. tsc clean.
- [x] **T-136** Instagram section UI scaffolding. **Sidebar** (`src/components/layout/Sidebar.tsx`): added a small `ChannelBadge` helper, "WA" pill on Broadcasts (NAV_ITEMS render) and on the Automations group header; new flat Instagram group below it with an "IG" badge and 4 sub-items with icons (Comments/`MessageCircle`, Story Replies/`CircleDot`, DM Keywords/`Keyboard`, Bot settings/`Bot`), gated by the existing `canShow()` role check (same pattern as Automations/Analytics/Settings - agents don't see it since `/dashboard/instagram` isn't in their allowed prefixes). **`InstagramGate`** (`src/components/instagram/InstagramGate.tsx`): fetches `GET /api/settings/instagram` on mount, shows a skeleton while loading, a centered "connect your account" card linking to `?open=instagram` when not connected, and renders children when connected. **Stub pages**: created the 4 routes (`comments`, `story-replies`, `dm-keywords`, `bot`), each wrapping a `Header` in `InstagramGate`. UI-only per spec, no TDD. tsc clean. Full suite 333/333 pass (zero regressions) - note: required running vitest under Node 26 (Homebrew), the shell's default `node` on PATH is v16 and too old for this project's tooling; flagged to Rutvik to fix PATH.
- [x] **T-135** Self-review: Instagram channel plumbing (T-127..T-130). Reviewed and verified all aspects, tests passed (333/333) and added missing decision log entry for "one Instagram handle per org".
- [x] **T-130** Instagram integrations settings UI. Added `useInstagramConnection` hook and Instagram `IntegrationTile` to `src/app/dashboard/settings/integrations/page.tsx`. Created `InstagramConnectionCard.tsx`, `InstagramConnected.tsx`, and `InstagramDisconnected.tsx` reusing the existing UI components and layouts. Replaced the missing Instagram icon from `lucide-react` with `Camera`. Also fixed broken mock expectations in `src/app/api/shopify/proxy/route.test.ts` regarding a previous removal of WhatsApp opt-in messages.
- [x] **T-129** Instagram inbound webhook parsing. Extended `src/app/api/webhooks/meta/route.ts` to process `instagram` payloads. Extracts sender ID and message text, resolves the `org_id` via `instagram_connections`, creates or finds the conversation thread using `findOrCreateConversation`, and upserts the message into the `messages` table with `channel: 'instagram'`. Tested via new `src/app/api/webhooks/meta/route.test.ts` (mocked Supabase and helper methods). Also fixed an unrelated failing test in `src/app/api/shopify/proxy/route.test.ts`.
- [x] **T-128** `InstagramAdapter` implementing `IChannelAdapter`. New `src/lib/channels/instagram/InstagramAdapter.ts` - constructor loads `access_token` + `instagram_user_id` from `instagram_connections` (`org_id` + `is_active = true`); `sendText(to, body)` POSTs to `https://graph.facebook.com/v18.0/{instagram_user_id}/messages` (`access_token` as a query param, never in the body) with `{ recipient: { id: to }, message: { text: body } }`, returning `ChannelSendResult` to match the `IChannelAdapter` contract exactly (same shape as `WhatsAppAdapter`). No active connection or a 4xx from Graph both return an error result rather than throwing. Registered in `getChannelAdapter` for `'instagram'` (registry test updated - instagram now resolves an instance instead of throwing "not yet implemented"). TDD: 4 new adapter tests + updated registry test, 10/10 channels tests green. Full suite 327 pass, same 5 pre-existing shopify-proxy failures. tsc clean.
- [x] **T-127** Instagram OAuth credentials storage. Migration `20260622_instagram_connections.sql` adds `instagram_connections` (mirrors `shiprocket_connections` pattern: one row per org via `unique(org_id)`, RLS via `org_members`). New `src/lib/instagram/oauth.ts` - pure-HTTP Graph API helpers (`exchangeCodeForToken`, `exchangeForLongLivedToken`, `fetchInstagramProfile` which walks `/me/accounts` Pages to find the linked Instagram Business account, same pattern as `@/lib/whatsapp/embeddedSignup`). New `src/lib/instagram/connect.ts` exports `connectInstagramAccount(supabase, orgId, code)` - the shared exchange+upsert flow used by both entry points so the logic isn't duplicated. Routes: `GET /api/settings/instagram` (status only - handle/profilePicUrl/isConnected, never the access token), `POST /api/settings/instagram/connect` (browser-initiated, `{code}` body), `DELETE /api/settings/instagram/disconnect` (`is_active = false`), `GET /api/auth/instagram/callback` (Meta's OAuth redirect target, delegates to the same `connectInstagramAccount`). Added `instagram_connections` to `src/types/database.ts`. TDD: 21 new tests across oauth/connect/all four routes, all green. Full suite 323 pass, same 5 pre-existing shopify-proxy failures. tsc clean. **Blocked on a manual prerequisite**: Rutvik must add Instagram scopes (`instagram_manage_messages`, `instagram_manage_comments`, `pages_show_list`, `instagram_basic`) to the existing Facebook app in Meta App Dashboard before this flow can be exercised live - code is otherwise complete and unit-tested against mocked Graph API responses. No new env vars needed (reuses `NEXT_PUBLIC_META_APP_ID`, `META_APP_SECRET`, `NEXT_PUBLIC_APP_URL`). No UI yet - the integrations settings card lands in T-130 per roadmap.
- [x] **T-126** Self-review: channel abstraction (T-121 to T-125) (M6). **Tests**: full suite 302 pass / 5 fail (the pre-existing shopify-proxy lead_capture failures, unrelated) - zero regressions. **Abstraction boundary**: `sendWhatsAppText` outside `lib/whatsapp/` + `channels/adapters/whatsapp/` exists only in `webhooks/meta/route.ts` (COD/opt-out transactional replies, intentionally raw - not part of the inbox/bot send path) and `process-broadcasts/route.ts` (intentionally WhatsApp-only per the T-117 decision log entry); confirmed absent from the inbox reply route and `BotService`. **Automations untouched**: `git diff` on `process-automations/route.ts` and `sendTemplate.ts` from before T-121 to now is empty, and neither imports `channels/` - confirmed WhatsApp-only. **Channel tagging**: code-level check (no live DB access this session) - `findOrCreateConversation` and the `inbox/new` message insert don't explicitly set `channel`, relying on the migration's `DEFAULT 'whatsapp'`; correct today since WhatsAppAdapter is the only implementation, but flagged as a watch-item to set explicitly once Instagram sends exist. **Reply route manual test**: blocked - auth-gated dashboard + the T-121 migration not yet applied in DB, same constraint noted throughout this epic. **Living docs**: added a "Channel Abstraction (T-121..T-125)" entry to `knowledge.md`; the scoping decision (inbox/bot-only, templates stay WhatsApp-only) was already logged in `decision-log.md` under T-121, no duplicate entry needed.
- [x] **T-125** Channel abstraction - inbox UI channel indicators (M6). New `ChannelIcon.tsx` (flat single-color inline SVG, WhatsApp green / Instagram magenta outline, defaults to WhatsApp for null/undefined since pre-T-121 rows were backfilled to whatsapp). Wired into two surfaces: `ConversationItem.tsx` (small 14px circular badge in the bottom-left corner of the avatar, opposite the existing "needs attention" dot) and `ConversationHeader.tsx` (inline next to the customer name). Subtle, reference-only per spec. UI-only, no TDD. Verified: tsc clean, dev server boots with no build errors. Live render not exercised - inbox is auth-gated and needs the T-121 `channel` migration applied in DB.
- [x] **T-124** Channel abstraction - wire bot send path through channel adapter (M6). `BotService` funnels all outbound bot responses through a single private `sendToCustomer`; refactored it to call `getChannelAdapter(channel, brandId, supabase).sendText(phone, text)` instead of resolving WhatsApp credentials + `sendWhatsAppText` directly. Credential resolution now lives entirely in the adapter, so the bot is channel-agnostic - when an Instagram adapter exists, the bot works for Instagram with no further change. Dropped the `@/lib/whatsapp/client` import from `BotService`; the 4 `sendToCustomer` call sites now pass `brandId` instead of the `org` creds object. A missing adapter (e.g. instagram, which throws "not yet implemented") or a send failure is caught + logged so the fire-and-forget bot path never crashes. Bot logic, tool calling, and escalation untouched - only the final send step changed. TDD: new `BotService.test.ts` - 3 tests (routes through adapter forwarding channel+orgId, forwards instagram channel, does not throw when no adapter exists). Boundary check: `sendWhatsAppText` no longer appears in BotService or the inbox reply route. Full suite 302 pass, same 5 pre-existing shopify-proxy failures (zero new regressions). tsc clean.
- [x] **T-123** Channel abstraction - Meta webhook channel detection (M6). New pure `src/app/api/webhooks/meta/detectChannel.ts` classifies a Meta payload as `'whatsapp'` (object `whatsapp_business_account` / `entry[].changes`) or `'instagram'` (object `instagram` / `entry[].messaging`), returning null for anything unrecognized. Wired into `webhooks/meta/route.ts` POST: detects the channel up front; unknown payloads log + return 200, Instagram payloads log "not yet handled" + return 200 (no IG inbound parsing/send path exists yet - tag-and-ack only), WhatsApp continues the existing flow. Threaded the detected `channel` through `handleInboundMessage` → stamped on the `conversations` upsert and the `messages` insert, and through `triggerBot` → `botService.handleInboundMessage` (replacing the hardcoded `'whatsapp'`). TDD: 5 detectChannel tests (whatsapp, instagram, two structural fallbacks, unknown→null). Full suite 299 pass, same 5 pre-existing shopify-proxy failures (zero new regressions). tsc clean.
- [x] **T-122** Channel abstraction - WhatsAppAdapter + wire inbox reply route (M6). New `src/lib/channels/adapters/whatsapp/WhatsAppAdapter.ts` implements `IChannelAdapter.sendText` - loads `waba_id`/`meta_access_token`/`phone_number_id` from `organizations` by orgId internally, resolves the phone number id, and delegates to `sendWhatsAppText` (`@/lib/whatsapp/client`); returns `{messageId, error}` (missing creds or unresolvable phone → error result, no send). Registry now returns a `WhatsAppAdapter` for `'whatsapp'` (instagram still throws "not yet implemented"). Refactored `src/app/api/inbox/reply/route.ts` to be channel-agnostic: reads `conversation.channel`, calls `getChannelAdapter(channel, orgId, admin).sendText()`, and removed all WhatsApp-specific credential fetching/phone resolution from the route (that logic now lives in the adapter); the message insert also stamps `channel`. Pure WhatsApp behaviour-preserving refactor. TDD: 3 adapter tests (loads creds + sends, missing creds → error, unresolvable phone → error) + updated registry test (whatsapp returns instance, instagram throws). Full suite 294 pass, same 5 pre-existing shopify-proxy failures (zero new regressions). tsc clean.
- [x] **T-121** Channel abstraction - foundation: migration + IChannelAdapter + registry (M6). Migration `20260622_channel_column.sql` (additive): `channel text NOT NULL DEFAULT 'whatsapp' CHECK (channel IN ('whatsapp','instagram'))` on both `conversations` and `messages`, all existing rows default to whatsapp. New `src/lib/channels/`: `types.ts` (`Channel` union + `ChannelSendResult`), `IChannelAdapter.ts` (minimal `sendText(to, body)` + optional `sendMedia?()` - deliberately NO template sending; templates stay WhatsApp-only in `sendWhatsAppTemplate`), `registry.ts` (`getChannelAdapter(channel, orgId, supabase)` - shell mirroring the T-109 shipping registry). Updated `src/types/database.ts` to add `channel` to conversations + messages Row/Insert/Update; added `channel: 'whatsapp'` to the optimistic message in `useReply.ts`. No adapter implementations yet (WhatsAppAdapter lands in T-122), so the registry recognizes whatsapp/instagram as known channels but throws "not yet implemented" for both and "Unsupported channel" for anything else - same shape as T-109's delhivery stub. TDD: 3 registry tests (unknown channel throws, whatsapp/instagram recognized). Zero changes to automations, broadcasts, or sendTemplate (verified). tsc clean. Migration applied via Supabase dashboard SQL editor (not auto-applied).
- [x] **T-120** Self-review: unified timeline implementation (T-116 to T-119). Verified migration safety, inbox graduation, backward compatibility, and badge rendering.

## 2026-06-21
- [x] **T-120** Fix missing `sender_type` on agent-initiated message in `inbox/new`. The outbound message insert in `src/app/api/inbox/new/route.ts` didn't set `sender_type`, so the `messages` default `'customer'` was stored on an `'outbound'` row - inconsistent and would mis-render under T-119's badge logic (which renders bubbles by `sender_type`). Set `sender_type: 'agent'` to match the pattern in `inbox/reply/route.ts`. Extended `inbox/new/route.test.ts` to capture message inserts and assert `direction: 'outbound'` + `sender_type: 'agent'` (2 tests pass). tsc clean.
- [x] **T-119** Unified timeline - automated message badge UI. `MessageBubble.tsx` now detects `sender_type = 'automation'` and renders a muted, right-aligned bubble (`var(--cream-2)` background, 1px `var(--border)`, dark text) with a small labelled chip showing `source_label` ("Order Confirmation", "Shipping Update: Out for Delivery", "Broadcast: Summer Sale") so agents can tell auto-sends from typed agent replies at a glance. No agent avatar/name to suppress - the thread renderer (`ConversationThread.tsx`) only renders `MessageBubble` per message, with no per-message avatar/name. Updated the generated DB types (`src/types/database.ts`): added `'automation'` to `messages.sender_type` union, `messages.source_label`, and `conversations.inbox_visible` across Row/Insert/Update; added `source_label: null` to the optimistic message in `useReply.ts` to satisfy the now-required Row field. UI-only, no TDD per spec. Verified: tsc clean (filtered recharts), dev server boots with no build errors; full live render is auth-gated (login + the T-116 migration applied in DB required) so the rendered bubble couldn't be exercised this session. Completes the unified-timeline epic (T-116..T-119).
- [x] **T-118** Unified timeline - inbox visibility graduation rules. **Inbox list**: `useConversations.ts` now filters `.eq('inbox_visible', true)` so automation-only threads stay hidden (T-116 backfilled active threads to true, so nothing disappears). **Customer initiated**: Meta webhook (`webhooks/meta/route.ts`) sets `inbox_visible: true` in the conversation upsert when an inbound message arrives. **Agent manual reply**: `inbox/reply/route.ts` sets `inbox_visible: true` on the conversation update (alongside the existing bot-handover). **New conversation (NewConversationModal)**: the modal POSTs to `/api/inbox/new`, so the fix lives there - replaced the inline find-or-create block with the shared `findOrCreateConversation` (reuses any existing thread incl. a hidden automation-only one rather than duplicating) and the post-send update now sets `inbox_visible: true, status: 'open'`. TDD: new `inbox/new/route.test.ts` asserts the route delegates to `findOrCreateConversation` with the normalized phone and graduates the thread (`inbox_visible: true`); the reuse-existing behavior itself is already unit-covered by the T-116 findOrCreateConversation tests. Full suite 287 pass, same 5 pre-existing shopify-proxy lead_capture failures (zero new regressions). Browser verification deferred (auth-gated dashboard + migration not yet applied in DB). Next: T-119 (automation badge UI).
- [x] **T-117** Unified timeline - wire senders to write to the timeline. Added optional `{ supabase, orgId, sourceLabel }` to the `sendWhatsAppTemplate` wrapper (`src/lib/whatsapp/sendTemplate.ts`); on a successful send with all three present it records the message on the customer's timeline. Extracted the write into a shared `src/lib/conversations/recordTimelineMessage.ts` (find-or-create conversation, insert `messages` row: `direction: 'outbound'`, `sender_type: 'automation'`, `source_label`, `body` = template name, `status: 'sent'`, `meta_message_id`) so any sender can reuse it; errors are swallowed (logged) so a timeline failure never breaks a send. New `src/lib/conversations/sourceLabels.ts` maps automation `event_type` and shipping `eventType` to friendly badge labels ("Order Confirmation", "Shipping Update: Out for Delivery"). **Call sites wired**: automations (`process-automations/route.ts`, via wrapper params), shipping webhook dispatch (`shipping/webhook/[provider]/[orgId]/route.ts`, via wrapper params), and broadcasts (`process-broadcasts/route.ts`). Note: broadcast does NOT use the wrapper - it sends via the raw `@/lib/whatsapp/client` `sendWhatsAppTemplate`/`sendWhatsAppText`, so it calls `recordTimelineMessage` directly at its success branch (added `name` to the broadcasts select for the "Broadcast: <name>" label). Backward-compatible: callers that don't thread the new params behave exactly as before. TDD: 16 tests across findOrCreateConversation, recordTimelineMessage, sourceLabels, and sendTemplate (success writes row / failed send writes nothing / missing params skips silently). Full suite 286 pass, the same 5 pre-existing shopify-proxy lead_capture failures as T-115 (zero new regressions). Next: T-118 (inbox visibility graduation rules).
- [x] **T-116** Unified timeline - foundation: migration + find-or-create utility. Migration `20260621_unified_timeline_foundation.sql` (additive, no data loss): added `conversations.inbox_visible boolean NOT NULL DEFAULT false`; extended `messages.sender_type` CHECK to include `'automation'` (drop + re-add, preserves customer/agent/bot/system); added nullable `messages.source_label text` for badge text. Backfill sets `inbox_visible = true` for every conversation that already has an inbound message so the existing inbox doesn't break. Created `src/lib/conversations/findOrCreateConversation.ts` - the primitive every sender calls: looks up by org_id + customer_phone, returns existing or inserts a hidden (`inbox_visible = false`) thread; race-safe via the UNIQUE(org_id, customer_phone) constraint (catches unique violation, re-fetches winner). TDD: 3 tests (returns existing, creates hidden, recovers from concurrent insert), all green. Migration must be applied via Supabase dashboard SQL editor (`supabase db push` not configured). Next: T-117 wires senders through `sendWhatsAppTemplate`.
- [x] **T-115** Self-review of the shipping adapter implementation (T-109..T-114). Ran the full suite: 274 pass, the only 5 failures are pre-existing in `src/app/api/shopify/proxy/route.test.ts` (lead_capture discount tests, untouched by shipping work) - zero regressions. **Provider agnosticism**: confirmed no `@/lib/shiprocket` imports leak into dispatch/business logic; the only importers are `src/app/api/shiprocket/connect/route.ts` and the integration settings page, both of which are provider-specific *connection setup* (the allowed "one settings UI per provider" per CLAUDE.md), not the agnostic dispatch path - not bugs. **UI labels**: the only provider names left in dashboard UI are on the Integrations connection card itself (legitimate - that's where you connect Shiprocket specifically). Found one stale label: `OnboardingModal.tsx` still said "Order Notifications" (the pre-T-114 name) - renamed to "Order & Shipping Events" with an updated description. **Adapter compliance**: `ShiprocketAdapter` implements `IShippingAdapter` with no `any` casts at the boundary. **Dedup**: traced and test-covered - org_id + event_type + status='sent' + AWB-in-template_variables means a duplicate webhook produces exactly one queue row and one send. **Org resolution**: orgId comes from the URL path and the order lookup is scoped `.eq('org_id', orgId)`, so no cross-tenant leak; the order-number collision limitation is closed (not just noted) by the org-scoped URL, already logged in decision-log. **Migration**: the CHECK constraint adds the 3 shipping types and preserves all 11 pre-existing event values, dropping none. **Living docs**: knowledge.md, roadmap.md, done.md all confirmed updated.
- [x] **T-114** Order Notifications UI - stats, log, and navigation updates. **Sidebar**: renamed "Order Notifications" to "Order & Shipping Events" in `Sidebar.tsx`. **Page header**: subtitle now covers the full journey (placed, shipped, out for delivery, delivered, failed). **Stats card**: extended `OrderNotifStats` with `outForDeliverySentThisMonth`/`deliveredSentThisMonth`/`deliveryFailedSentThisMonth`; the card now shows 6 tiles in a 3-col grid (Confirmations, Shipping updates, Out for Delivery, Delivered, Failed Delivery, Total) - dropped the old "Avg delivery days" placeholder tile, which always rendered "-" (the hook never computed it; comment said "computed from orders table in future"). **Message log**: `useOrderNotificationLog` now queries all five event types via a shared `ORDER_NOTIF_EVENT_TYPES` const (used by both the fetch and the realtime filter), `OrderNotifEventType`/`OrderNotifFilter` widened, tracking column falls back to `template_variables.awb` for shipping events; `OrderNotificationLogRow` got type badges for the three shipping events ("Out for Delivery", "Delivered", "Failed Delivery"); `OrderNotificationLog` got matching filter tabs. Visual verification blocked (auth-gated dashboard, no credentials this session); verified via `tsc --noEmit` clean + suite green + dev server compiling with no errors. Existing order_confirmed/order_shipped rows unaffected.
- [x] **T-113** Order Notifications UI - shipping events settings section. Split the page into two labelled sections via a new `SectionLabel` divider: "Order Events" (existing confirmation/shipping cards, untouched) and "Shipping Events" (new). New `ShippingEventsCard.tsx`: if no shipping provider is connected (checked by querying any active `shiprocket_connections` row for the org - provider-agnostic, so Delhivery lifts the gate by just OR-ing its table in), renders a soft "Connect a shipping provider to enable these" banner and stops; if connected, renders three event cards (Out for Delivery, Delivered, Failed Delivery / NDR), each with a toggle + the shared `AutomationTemplateCard` for template selection and slot mapping - same building block the existing order cards use. Added `updateShippingEvent(eventType, patch)` to `useOrderNotificationSettings` (merges into the `shipping_events` JSONB, guards against enabling without a template) plus `ShippingEventType`/`DEFAULT_SHIPPING_EVENT_CONFIG` exports. Supporting logic change: the slot-mapping editor reads `AUTOMATION_VARIABLE_REGISTRY[automationType]`, so added `out_for_delivery`/`delivered`/`delivery_failed` to the `AutomationEventType` union and registered a shared `SHIPPING_EVENT_VARIABLES` set (customer_name, order_number, awb, location, estimated_delivery, store_name) - matching exactly the `vars` the T-112 webhook dispatch builds, so a slot mapping configured here resolves correctly at send time. That registry change was TDD'd (2 new tests asserting the registry entries exist and resolve). Visual verification blocked: the dashboard is auth-gated and no login credentials were available in this session; verified instead via full `tsc --noEmit` (clean) + dev server compiling the route with no errors + the existing test suite. UI section work isolated from stats/log/sidebar (those are T-114).
- [x] **T-112** Shipping events settings storage and backend dispatch - the core value prop: shipping provider fires webhook, Tellero sends branded WhatsApp from the brand's own number instead of the provider's. **Settings**: `shipping_events` is a new key inside the existing `organizations.order_notification_settings` JSONB blob (turned out not to be its own table - same pattern as `slot_mappings`), keyed by `ShippingEventType`. Added `ShippingEventConfig`/`ShippingEventsSettings` types + default `{}` to `useOrderNotificationSettings.ts`. **One real migration**: `automation_queue.event_type`'s CHECK constraint widened to include `out_for_delivery`/`delivered`/`delivery_failed` (`supabase/migrations/20260621_shipping_events_queue_types.sql` - needs manual apply via Supabase dashboard SQL editor, same as always). **Dispatch**: extended the T-111 webhook route - resolves the order by `org_id` + `shopify_order_number`, loads the org's `shipping_events` config, skips silently if disabled/no template/no phone/no template row, dedups via an `automation_queue` lookup (`org_id` + `event_type` + `status='sent'` + `template_variables` containing the AWB), resolves params via the existing `resolveAutomationParams`/`resolveTemplateParams` helpers, sends directly via `sendWhatsAppTemplate` (no `automation_queue` pending row, no cron - by design), then inserts a `status: 'sent'` row so the existing Order Notifications log (and T-114) have a ready-made source. Mid-task design change (confirmed with Rutvik before building): the webhook URL became org-scoped (`/api/shipping/webhook/[provider]/[orgId]`, was `/api/shipping/webhook/[provider]` from T-111) to close a cross-tenant collision risk - the global URL had no way to disambiguate which org an order-number match belonged to. `ShiprocketConnected.tsx`'s webhook URL now reads `connection.brand_id` directly. 11 new/updated route tests, all passing.
- [x] **T-111** Unified shipping webhook route and end-to-end wiring. Added `src/app/api/shipping/webhook/[provider]/route.ts`: resolves the adapter via the registry (catching its throw for unknown/unimplemented providers as a `400`), parses the JSON body (`400` on parse failure), runs `adapter.verifyWebhook?()`, normalizes the payload, and logs the `NormalizedShippingEvent` (full WhatsApp dispatch lands in T-112). Wired `registry.ts` to actually return a `ShiprocketAdapter` instance for `'shiprocket'` now that T-110 built it - `'delhivery'` still throws "not yet implemented". Updated the displayed webhook URL in `ShiprocketConnected.tsx` (not `ShiprocketWebhookGuide.tsx`, which only renders setup-step text and takes the URL as a prop) from `/api/shiprocket/webhook` to `/api/shipping/webhook/shiprocket`. Left the existing stub at `src/app/api/shiprocket/webhook/route.ts` untouched. 4 new route tests + 1 updated registry test, all passing.
- [x] **T-110** ShiprocketAdapter - webhook normalization. Added `src/lib/shipping/adapters/shiprocket/ShiprocketAdapter.ts` implementing `IShippingAdapter`: `normalizeWebhook()` maps `ShiprocketWebhookPayload` to `NormalizedShippingEvent` (awb, channelOrderId, estimatedDelivery from `etd`, customerName/Phone, location from city+state joined). `verifyWebhook()` is a no-op returning `true` - Shiprocket has no HMAC signing, only IP allowlisting. `getTracking`/`getShippingEstimate` deliberately omitted (picked up in T-082). The status mapping lives in a separate `statusMap.ts`: pulled Shiprocket's real 90-code `shipment_status_id` table (sourced from a third-party enum that cites Shiprocket's official API docs directly, since the public docs site is JS-rendered and not fetchable) plus a string `current_status` fallback map, both bucketed into the 8 canonical `ShippingEventType`s. Two deliberate deviations from a naive lifecycle reading: codes 36 (Undelivered) and 46 (RTO Non-Delivery Report) bucket to `delivery_failed` rather than `exception`/`return_initiated`, since those are exactly the NDR signals the upcoming "Failed Delivery / NDR" feature (T-112/T-113) needs to catch. Unknown numeric codes fall back to the string map, then to `'exception'`. 20 new tests (11 statusMap + 9 adapter), all passing.
- [x] **T-109** Shipping adapter abstraction layer - canonical types and interface. Added `src/lib/shipping/types.ts` (`ShippingProvider`, `ShippingEventType`, `NormalizedShippingEvent`, `ShippingEstimate`), `src/lib/shipping/IShippingAdapter.ts` (adapter contract: `normalizeWebhook` required, `verifyWebhook`/`getTracking`/`getShippingEstimate` optional, constructor takes `brandId` + `supabase`), and `src/lib/shipping/registry.ts` (`getAdapter(provider, brandId, supabase)` factory). Registry currently throws for every provider - "Unsupported shipping provider" for unknown slugs, "not yet implemented" for known-but-unbuilt ones (`shiprocket`, `delhivery`) - since no adapter classes exist yet (lands in T-110). Confirmed this split with Rutvik before writing tests, since the todo's literal wording ("known provider returns instance") conflicted with the "no adapter implementations in this task" constraint. Zero changes to existing `src/lib/shiprocket/` files. 3 new tests, all passing.
- [x] **T-108** Built a new Checkout UI Extension (`order-tracking-ui`) specifically targeting the Thank You and Order Status pages. This uses React (`@shopify/ui-extensions-react/checkout`) to dynamically grab the `order.id` and `address.phone` directly from the checkout session API. It submits to the existing Tellero app proxy, which was updated to handle CORS `OPTIONS` preflight requests so the sandboxed checkout extension can securely communicate with it. The existing `order_tracking.liquid` theme block was left intact for use on standard storefront pages.
- [x] **T-107** Disabled sending WhatsApp messages on lead capture opt-in to streamline the workflow. Now, the system only generates the discount code via Shopify and displays it immediately on-screen as a copyable chip, regardless of whether a WhatsApp template is configured. The discount code generation logic was decoupled from the template lookup.
- [x] **T-106** Two post-launch fixes on the working widgets: the country-code `<select>` stayed visible alongside the success state after a working submit (only the phone input and button hid) - now hides on both lead-capture and order-tracking. Lead capture's discount code showed inline in the success sentence (`Code: <strong>X</strong>`) with no way to copy it; replaced with a proper code chip + Copy button (`navigator.clipboard.writeText`) so the customer doesn't have to rely on the WhatsApp message arriving to get the code. Restructured the success div's markup (wrapped the static text in its own `<span>`) so the JS only appends the code chip/button rather than overwriting the checkmark icon that was already there. Deployed as `tellero-20`. Three more polish requests: zeroed the default Padding (still inset versus the rest of the page; merchant-adjustable via the slider if wanted back), trimmed select/input/button height 42px->38px uniformly on both widgets, and removed the `placeholder_text` setting/attribute entirely on both - the default was already blank but an existing instance had a stale non-empty value baked into its saved settings_data from before that fix, which a schema default change alone can't override retroactively; removing the setting/attribute outright guarantees no instance (old or new) can ever show one again. Deployed as `tellero-21`.
- [x] **T-105** Storefront widgets only worked for US-format numbers (hardcoded `+1` placeholder). Added a shared `country_codes.liquid` snippet (~190 countries, dial codes) and a country `<select>` next to the phone input on both the lead-capture and order-tracking widgets - submitted phone is now `+<dial><digits>`, with a per-block "Default Country" ISO-code setting merchants set in the theme editor. Also added a Width range setting (280-900px) to both, since they previously always stretched to full section width. Dropped order-tracking's `value="{{ customer.phone }}"` auto-fill since its format was ambiguous and would have doubled up with the new selector. Root-caused the "Something went wrong" submit failures from earlier today: the app (config + all 3 theme extension blocks) had never been pushed via `shopify app deploy` this session, so Shopify had no valid app-proxy mapping for `/apps/tellero` (404) and the theme editor was still showing pre-session schema. Deployed via `shopify app deploy --allow-updates` (`tellero-14` for the deploy-gap fix, `tellero-15` for the country/width feature). 404s persisted after `tellero-15` though - found a real bug in the country-select wiring: both widgets called `window.telleroPopulateCountrySelect()` *before* `addEventListener('submit', ...)`, so if that call ever threw, the script died before the listener attached, and the form fell back to a native submit (no `preventDefault`, no `action`) - hitting whatever URL the page/preview-iframe was actually on, not our fetch. Reordered (listener first, populate wrapped in try/catch) and redeployed as `tellero-16`. Follow-up visual bugs: Width setting wasn't constraining the block (no `box-sizing`/`width:100%` base, no fallback for a missing settings value) and the new `<select>` had no shared `box-sizing`/height with the `<input>`/`<button>`, making the row look skewed - fixed both with explicit `box-sizing: border-box`, a 42px height on all three controls, and `| default: 420` on `max_width`. Placeholder text default removed entirely (Shopify rejects `"default": ""` on text settings - "no default" achieves the same blank field) rather than building country-aware placeholder logic, per explicit instruction to leave it blank if not worth the complexity. Redeployed as `tellero-17`. 404 persisted even on the real published storefront (not just editor preview) - actual root cause found: Shopify's app proxy always appends a trailing slash forwarding to the upstream (`/api/shopify/proxy` -> `/api/shopify/proxy/`); Next.js's default trailing-slash redirect bounced that back through the proxy, and the browser resolved the relative `Location` header against the storefront domain instead of `tellero.in` - a 404 on the shop's own domain, never reaching our backend. Confirmed against an identical case on Shopify's dev community forum. Fixed with `skipTrailingSlashRedirect: true` in `next.config.mjs` - this is a backend config change, ships via Vercel on push, no `shopify app deploy` needed. Two more visual follow-ups: the Width default (420) was narrower than the surrounding column on a product-page sidebar, misaligning it against full-width sibling content there - bumped default to 900 (the slider's own max), since the block is already `width:100%` of its container, so on a narrow column it just fills the column and the cap only bites on wide full-bleed sections. Separately, `whatsapp_optin.liquid`'s phone input had `min-width: 0` and no `flex-wrap` on its row (unlike `order_tracking.liquid`, which already had both), so on mobile it collapsed to near-zero width fighting the select+button for space - added `flex-wrap: wrap` and `min-width: 140px` on the input, plus a `min-width: 90px` on the country select on both blocks. Redeployed as `tellero-18`. Two more polish fixes: lowered default Padding 24px->16px (still looked inset versus how flush sibling page content sat against the same column edge) and shortened the country select's option text from `"United States (+1)"` to `"US +1"` (full name moved to a `title` tooltip) since it was overflowing the select's width and getting cut off mid-dial-code - both in the shared `country_codes.liquid` snippet plus the two block schemas, deployed as `tellero-19`. Finally found the actual root cause of the lingering submit failures (was a 404 from the trailing-slash bug above; after that fix it became a 401): `verifyShopifyAppProxy()` in `src/lib/shopify/shopifyAuth.ts` built its HMAC message as `` `${key}${value}` `` per query param instead of Shopify's documented `` `${key}=${value}` `` (sorted, no delimiter between pairs) - missing `=` meant the computed signature never matched Shopify's real one, so every genuine app-proxy request was rejected as Unauthorized. This function had zero test coverage; added 3 tests (valid signature per the documented algorithm, invalid signature, missing signature) before fixing it. Backend fix, ships via Vercel on push.
- [x] **T-104** Fixed two storefront widget bugs. `order_tracking.liquid`'s submit handler referenced an undefined `phone` variable instead of `phoneInput.value` - every submit threw a `ReferenceError`, caught and surfaced as "Something went wrong. Please try again." (visible once T-103 made the block placeable on pages like Cart, where merchants actually started using it). Also fixed `whatsapp_optin.liquid`: the Description text had a hardcoded "10%" independent of the Discount % slider, so a merchant editing one without the other could show a different percentage than what's actually applied. Description now uses a `{{ discount }}` token that's filled in from the single `discount_percentage` setting, so there's one source of truth. (Follow-up same day: the token still let merchants type a conflicting number, so replaced `description_text` with `description_prefix`/`description_suffix` text fields - no field accepts a % at all now, it's always rendered from the slider.)

## 2026-06-20
- [x] **T-102** Wired up discount generation + WhatsApp send for the storefront WhatsApp Lead Capture widget. Previously the widget only captured the phone number, opted the contact in, and tagged the Shopify customer - the promised discount code and WhatsApp message were never sent. Discount % is now a merchant-editable field on the theme block itself (`whatsapp_optin.liquid`, `discount_percentage` range setting, clamped 1-50 server-side), passed to `/api/shopify/proxy` and minted via `shopifyClient.createDiscountCode`. Idempotent per contact via new `contacts.widget_discount_code`/`widget_discount_sent_at` columns - repeat opt-ins never mint a second code. WhatsApp template selection lives on a small new card on Settings > Shopify (`organizations.lead_capture_settings.template_id`), reusing the existing `discount_code` slot-label convention (`__DYNAMIC_DISCOUNT_CODE__` marker) already used by abandoned-cart/winback. Extracted the template-send wrapper from `process-automations/route.ts` into `src/lib/whatsapp/sendTemplate.ts` so the proxy route and the cron job share one implementation. The proxy response now also returns `discountCode` so the widget can flash it on-page instantly. TDD: new tests for `sendTemplate.ts` (4) and the proxy route's discount+send block (6) plus a non-interference check for `order_tracking`; full suite 240/240 green, tsc clean. New migration `20260620_lead_capture_widget_discount.sql` not yet applied to prod - needs manual run via Supabase dashboard SQL editor.
- [x] **T-103** Removed `templates: ["customers/order"]` restriction from the `order_tracking.liquid` Theme App Extension block. Shopify's "New Customer Accounts" feature completely deprecates the classic `customers/order` template, which was causing the block to be hidden in the Theme Editor for stores using the new accounts flow. Removing this restriction allows merchants to place the Order Tracking block on any available page (like the Cart page or a dedicated tracking page).

## 2026-06-19
- [x] **T-101** Fixed a bug where abandoned checkout UI wasn't showing records queued for sending because PostgREST schema inference failed on `onConflict` after `user_id` was renamed to `org_id`. Replaced `upsert` with explicit `select` then `update/insert` in both `checkouts-create` and `orders-create` webhooks to avoid unique constraint naming inference issues. Also fixed a UI visual bug where updating an abandoned cart kept the old `message_sent_at` timestamp showing the message as "Sent" before the new 60-minute timer completed, by resetting `message_sent_at` to `null` on cart updates.
- [x] **T-100** Fixed App Store installs failing with `connection_failed` (Postgres `23502`: `null value in column "user_id"`). The `shopify_connections.user_id` column is still present and NOT NULL in prod (it is intentionally retained for the installer audit trail per `20260603_org_level_integrations.sql`; the `DROP COLUMN` line in `20260616_rename_user_id_to_org_id.sql` was never applied and has now been removed from the migration file so a fresh DB matches prod). During the org_id refactor the install upserts stopped *setting* `user_id`, so every insert violated the NOT NULL constraint - affecting both the session-linked branch and the cold path. Fix: both `appstore/callback` upserts now set `user_id` (session = the logged-in installer, cold = the created/matched merchant user). No prod migration required. Also added a reusable structured flow logger (`src/lib/shopify/installLog.ts`, `[route] step=<step> status=<start|ok|fail>` lines, Postgres error code/message/details extracted, no secrets logged) and instrumented every step of `appstore/install` + `appstore/callback` so the flow is traceable in Vercel logs in prod. Removed the temporary `&debug=` URL surface added earlier. TDD: 5 logger tests; 229 suite green, tsc clean.
- [x] **T-099** Added error-banner rendering to the integrations page (`src/app/dashboard/settings/integrations/page.tsx`). The Shopify OAuth callback redirects back here with `?error=...` codes (e.g. `shop_already_connected` from the T-098 guard, and the pre-existing `connection_failed`), but this page read only `open` and silently swallowed `error` - merchants saw no explanation. Now shows a dismissable red banner with a human message per code (falls back to a generic message for unknown codes) and strips the `error` param from the URL so a refresh doesn't re-show it. UI change, visually verified (behind auth - Rutvik to confirm logged in). tsc clean, 224 tests green.
- [x] **T-098** Closed a data-integrity gap found during the T-097 review: the session-linked install branch in `appstore/callback/route.ts` upserted on `org_id` without checking whether the shop was already connected to a *different* org. Since DB uniqueness is `(org_id, shop_domain)` (not global on `shop_domain`), this could create two active rows for one shop, after which the webhook lookup (`orders-create/route.ts:29`, `.eq('shop_domain', …).single()`) throws and silently no-ops - orders stop syncing with no error. Added a guard that, when a logged-in user installs a shop already owned by another org, redirects to `?error=shop_already_connected` instead of creating the duplicate. Same-org reconnects still pass through. TDD: 2 tests (block different-org, allow same-org reconnect). Suite 224 green.
- [x] **T-097** Re-reviewed all of today's Shopify work (T-087 through T-096, range `58ae9f7..b67a11e`). `shopifyClient.ts`, `shopifyWebhooks.ts`, `proxy/route.ts`, `shop-redact/route.ts`, the App Store install/callback routes, and the UI swaps all verified clean (no dangling refs to the deleted oauth routes, consistent env-var fallback, 224 tests green, tsc clean). One finding surfaced - the cross-org duplicate-shop gap in the session-linked install branch - fixed under T-098. Manual dev-store QA (cold install, session-linked install, reconnect, COD->Prepaid, bot tools, full sync) remains a pre-submission step that must be run against a real Shopify dev store; it cannot be done in-repo.
- [x] **T-096** Fixed the `tags` array-vs-string inconsistency in the prepaid-conversion detection path. `orders-create/route.ts:40` did `order.tags.includes('tellero_prepaid_conversion')` on the raw webhook payload (a comma-joined string), which both assumed string shape and substring-matched (a tag like `tellero_prepaid_conversion_archived` would false-positive). Added `hasOrderTag(tags, tag)` helper (`src/lib/shopify/orderTags.ts`) that normalizes both the webhook string shape and the GraphQL `string[]` shape to a trimmed exact-match array check, and wired it into the route. TDD: 5 helper tests (both shapes, whitespace, substring-guard, null/empty). Suite 222 green, tsc clean.
- [x] **T-095** Filled the `FULFILLMENT_STATUS_MAP` enum gaps in `ShopifyClient`. GraphQL's `IN_PROGRESS`, `PENDING_FULFILLMENT`, `ON_HOLD`, `SCHEDULED`, `OPEN`, and `UNFULFILLED` were unmapped and fell through to `null`, which the DB then stored as `'unfulfilled'` - making in-progress orders look cancellable to the bot (`tools.ts:129,196` only block cancel/address-change when status is `'unfulfilled'`/`null`). Mapped the in-progress family to distinct non-cancellable values (`in_progress`/`on_hold`/`scheduled`/`open`) and `UNFULFILLED` explicitly to `unfulfilled`; extended the `ShopifyOrder['fulfillment_status']` union to match. No bot-logic change needed - the bot already blocks any non-unfulfilled value. TDD: 1 parametric test covering all new states. Suite 217 green, tsc clean.
- [x] **T-094** Backfilled `gateway` / `payment_gateway_names` in `ShopifyClient.mapOrder` (were hardcoded `null`/`[]` after the REST→GraphQL migration). Added `paymentGatewayNames` to `ORDER_FIELDS`; `payment_gateway_names` now maps the full array and `gateway` takes the first element (REST exposed a single gateway string; GraphQL only has the array). TDD: 1 test asserting the mapping. No current caller depended on these (webhook handlers parse raw payloads), so it's an additive fix unblocking future `getOrders`-based payment-method checks.
- [x] **T-093** Migrated `src/app/api/shopify/sync/route.ts` off raw REST (was still on pinned `2024-01` via a local `shopifyFetch` helper, missed in the T-087/T-091 GraphQL migration) onto `ShopifyClient` GraphQL. Added `ShopifyClient.getCustomers` (cursor-paginated, returns new `ShopifyCustomerSummary` with `orders_count`/`total_spent`) and extended `getOrders` with a `created_at_min` filter, `after` cursor, and `pageInfo` return (additive - sole existing caller unaffected). Dropped REST link-header pagination + `status=any` (GraphQL orders connection returns all statuses by default; REST needed it because it defaulted to open-only). TDD: 2 ShopifyClient tests (getCustomers mapping/pagination, getOrders cursor+date filter) + 2 sync-route tests (agent 403 guard, customer/order pagination + upsert shape). Full suite 215 passing, tsc clean.
- [x] **T-087** Migrated `ShopifyClient` from REST Admin API to GraphQL Admin API (`orderCancel`, `orderUpdate`, `discountCodeBasicCreate`, `draftOrderCreate`, products/orders queries) to resolve Shopify App Store review item 2.2.4. All public method signatures and return shapes unchanged (GIDs unwrapped back to numeric ids) so callers needed no changes. Test suite rewritten for GraphQL request shapes, 19 passing.
- [x] **T-088** Fixed stale `user_id` column reference in `src/app/api/shopify/proxy/route.ts` orders update (renamed to `org_id` in migration `20260616_rename_user_id_to_org_id.sql`). Order-tracking opt-ins were silently failing to link to their order.
- [x] **T-089** Added `findCustomerByPhone` / `addCustomerTags` to `ShopifyClient` and wired the Shopify proxy route to tag the matching Shopify customer (`whatsapp-optin` / `whatsapp-order-tracking-optin`) on opt-in, so storefront-collected consent data is visible to the merchant in Shopify Admin. Resolves App Store review item 5.1.5. Best-effort, swallows sync failures so the contact upsert still succeeds.
- [x] **T-090** Migrated `shopifyWebhooks.ts` webhook management (register/list/delete subscriptions) from REST to GraphQL Admin API (`webhookSubscriptionCreate`, `webhookSubscriptionDelete`, `webhookSubscriptions` query), completing the REST→GraphQL elimination started in T-087. Topic strings mapped to GraphQL's UPPER_SNAKE_CASE enum; numeric webhook ids preserved via gid unwrap so the DB-stored `webhook_ids` shape is unchanged.
- [x] **T-091** Fixed `shop/redact` webhook (`src/app/api/shopify/webhooks/shop-redact/route.ts`) to actually delete shop data instead of only the `shopify_connections` row. Now also deletes `orders` and `abandoned_checkouts` scoped by `org_id` (both exclusively Shopify-sourced); `cod_confirmations`/`automation_queue` cascade automatically via FK. `contacts` deliberately left untouched - shared across non-Shopify channels. Resolves App Store privacy webhook compliance gap.
- [x] **T-092** Made the Shopify install flow App Store-review compliant (rule 2.3.1). Removed the manual `xxx.myshopify.com` entry field from `ShopifyDisconnected.tsx` (the literal violation) and replaced it with a CTA to the App Store listing (`NEXT_PUBLIC_SHOPIFY_APP_LISTING_URL`), so shop selection happens on Shopify's own surface. Added session-aware linking: `appstore/install` now stamps a `shopify_appstore_user_id` cookie when a logged-in Tellero user starts the install, and `appstore/callback` links the resulting connection to that user's org (skipping the email-matching account-creation path) when the cookie is present. Cold listing installs never set the cookie, so the existing anonymous magic-link login flow is unchanged. TDD: 4 install-route + 2 callback tests (including a cold-install fall-through regression test).

## 2026-06-18
- [x] **T-080** Built the `check_refund_status`, `log_customer_feedback`, and `search_product_availability` AI tools. Created the `customer_feedback` database table migration to securely store feedback. Updated `ShopifyClient` with a search method to fetch exact variant inventory counts. Appended these features to the `withErrorLogging` loop and updated the system prompt context.
- [x] **T-079** Built the `update_shipping_address` and `initiate_return` AI tools. Updated `ShopifyClient` with methods to securely update addresses via `PUT` request and append return requests as order notes. Integrated them seamlessly into the AI bot's `withErrorLogging` loop and updated the system prompt to grant the AI explicit permission to execute them.
- [x] **T-074** Implemented `get_order_status` function calling for the AI Support Bot to handle WISMO queries natively.
- [x] **T-078** Implemented DRY `withErrorLogging` wrapper for all AI tools (`src/lib/bot/tools.ts`) that safely logs tool failures to Vercel via `console.error` and injects them as internal system messages directly into the database chat timeline, ensuring AI failures are instantly visible to agents taking over the conversation. Also fixed a critical crash in the `BotTestWidget` by ensuring a mock conversation ID (`preview`) is supplied to prevent database insertion panics during testing.
- [x] **T-077** Built the `cancel_unfulfilled_order` AI tool. It securely queries the database to ensure the order belongs to the customer and is strictly 'unfulfilled', connects to the brand's Shopify instance using their stored API credentials, issues a live cancellation command, and provides COD-aware refund context back to the AI.
- [x] **T-076** Built the `escalate_to_human` AI tool, offering a formalized, robust way for the AI to seamlessly pass the conversation to a human agent, replacing the legacy `[HANDOVER]` text matching hack. The tool instantly flags the conversation in the database and mutes the bot.
- [x] **T-075** Updated the AI bot's system prompt rules. Previously, the strict "Only answer based on the knowledge above" rule was causing the AI to refuse to use the newly implemented order status tools. The rules now explicitly grant the AI permission to use tools for fetching live data when applicable.
- [x] **T-073** Fixed a Gemini API crash (`First content should be with role 'user'`) in the bot test widget by filtering out the initial hardcoded bot welcome message from the chat history payload before sending it to the AI endpoint.
- [x] **T-072** Swapped the UI alignment of the simulated chat in the bot test widget to present from the customer's perspective. The user's typed messages now appear on the right (`outbound`), and the bot's responses appear on the left (`inbound`).
- [x] **T-071** Reverted the database persistence of test messages to prevent analytics and inbox pollution, but retained the context window feature by passing the local frontend chat history to the AI endpoint.
- [x] **T-069** Upgraded the bot test widget (`BotTestWidget.tsx`) to an interactive mini chat simulation, reusing the real `MessageBubble` and `ReplyBox` components from the Inbox to provide a realistic preview of the bot's behavior without impacting production data.
- [x] **T-068** Reverted the inline display of brand details within the "Prefill from your website" card to keep it clean. Fixed a React state bug where the asynchronously loaded website URL was not successfully hydrating into the prefill URL list.
- [x] **T-067** Added visual indicators for prefetched brand details in the AI Bot prefill card and the knowledge base form so users are aware these fields are fetched from the brand profile and used as AI context.
- [x] **T-066** Updated the global onboarding flow (`OnboardingModal.tsx`) to pull the brand's website, support email, and Instagram handle from the profile and supply them to the AI bot prefill endpoint. Changed prefill action to append data rather than overwrite.
- [x] **T-065** Committed and pushed database migration (`20260618_bot_settings_brand_fields.sql`) for adding `website`, `instagram_handle`, and `support_email` to the `brand_knowledge` table.
- [x] **T-064** Passed prefetched `support_email` and `instagram_handle` to the AI prefill endpoint (`/api/bot/prefill/route.ts`) to enrich the AI extraction prompt with additional brand context.
- [x] **T-063** Updated AI prefill functionality (`BotSettingsPage.tsx`) to append new knowledge base content using double newlines instead of overwriting existing data.
- [x] **T-062** Added language dropdown to `TemplateDetailModal.tsx` allowing users to edit the language of draft templates directly. Updated `PUT /api/templates/[id]` to persist language changes.
- [x] **T-061** Fix "invalid param" error when submitting Hinglish templates to Meta. Root cause: `en-hinglish` is not a valid Meta language code. Removed `en-hinglish` entirely from the template editor (`templateHelpers.ts`) so users must explicitly choose English or Hindi. Also added detailed debug logging to both `metaTemplateApi.ts` and `client.ts`: full request payloads, Meta error codes/subcodes/fbtrace_id on failure.
- [x] **T-060** Built custom discount percentage configuration into Abandoned Cart and Winback Settings UI, dynamically reading it during the cron job to create precise Shopify discounts.
- [x] **T-059** Implement Dynamic Discount Codes via cron. Added `write_price_rules` and `write_discounts` to Shopify scopes. Built `createDiscountCode` to generate a dynamic `-10%` Shopify discount. Updated `process-automations` to dynamically substitute `__DYNAMIC_DISCOUNT_CODE__` variables by calling Shopify exactly at send time.
- [x] **T-058** Fix Vercel build failure by removing platform-specific `vitest` dependency (`@rolldown/binding-darwin-arm64`) that was crashing the Linux build environment.
- [x] **T-057** Add `product_image_url` variable to Abandoned Cart and Reorder automations. Extracted product image URLs from checkouts and line items in webhooks and registered them in `templateInjection.ts`.

## 2026-06-16
- [x] **T-041** Fix all Shopify webhooks crashing due to duplicate `org_id` queries (Ad-hoc). A previous global find-and-replace of `user_id` -> `org_id` accidentally changed `select('org_id, user_id')` to `select('org_id, org_id')` across all 10 Shopify webhook handlers. This caused PostgREST to throw an error, aborting the webhook execution entirely (so orders, customers, and checkouts weren't saving, and automations weren't queueing). Fixed all files to correctly query `select('org_id')`.
- [x] **T-040** Sweep of `user_id` -> `org_id` remaining references (Ad-hoc). Found and fixed several issues left over from the column rename:
  - Fixed `seed_automations` RPC failing on new signups because it was trying to `INSERT INTO automations (user_id)`.
  - Fixed `useConversations` and `useFlaggedCODCount` realtime subscriptions which were still filtering by `user_id=eq...` (crashing realtime).
  - Fixed `onConflict: 'user_id,phone'` in `src/app/dashboard/contacts/page.tsx` and `AddContactModal.tsx` which would crash when importing/adding contacts.
  - Fixed `onConflict: 'user_id,customer_phone'` in `src/app/api/webhooks/meta/route.ts` which would crash when upserting conversations.
  - Fixed `src/app/api/shopify/proxy/route.ts` which was selecting and upserting with `user_id`.
- [x] **T-038** Fix `failed_at` crash in AutomationQueueLog (Ad-hoc). Removed `failed_at` from the `useAutomationQueueLog` SQL select statement, as the `automation_queue` table tracks failures exclusively via `status='failed'` and `error_message`, unlike the `broadcast_recipients` table.
- [x] **T-037** Repair UI Log Hooks broken by `user_id` -> `org_id` rename (Ad-hoc). The `useOrderNotificationLog`, `useAbandonedCartLog`, `useCODLog`, and `useReorderLog` hooks were still querying the old `user_id` column and attempting to set up Supabase Realtime subscriptions with `filter: user_id=eq...`, causing crashes and missing realtime updates on the automation dashboards. Replaced all references with `org_id`.
- [x] **T-036** Missing Automation Logs (Ad-hoc). Built a reusable `AutomationQueueLog` component and `useAutomationQueueLog` hook that directly queries the `automation_queue` table. Injected it into the Welcome Flow, Winback Campaigns, and Review Requests pages so merchants can now see exactly who is receiving these messages, their delivery status, and any errors.
- [x] **T-035** Broadcast Sending Parallelization (Ad-hoc). Upgraded the `process-broadcasts` cron endpoint to process recipients in parallel chunks (`Promise.all`) instead of sequentially. Increased the max `BATCH_SIZE` from 100 to 500, quintupling the per-minute throughput to ~60,000/hr.
- [x] **T-034** Broadcast Detail Modal Polish (Ad-hoc). Replaced the rigid full-screen height constraint (`h-[calc(100vh-32px)]`) on the analytics DetailModal with `max-h` and `h-fit` so it dynamically shrinks to fit its content without excessive whitespace.
- [x] **T-033** Broadcast Reply and Opt-out Tracking (Ad-hoc). Added `mark_broadcast_replied` and `mark_broadcast_opted_out` RPCs to Supabase to reliably update the `broadcast_recipients` status based on inbound messages. Integrated these RPC calls into the Meta webhook handler (`handleOptOut` and the general inbound reply tracking block) to accurately reflect replies and opt-outs on the broadcast analytics UI without counter drift.
- [x] **T-032** Fix intermittent loading spinner flash on broadcasts page (Ad-hoc). Wrapped `useSearchParams()` in an explicit `<Suspense>` boundary to prevent Next.js App Router from auto-wrapping the whole page, which caused re-mounts resetting state. Added a `hasLoaded` ref so the spinner only shows on the genuine first load, not on Suspense re-suspensions.
- [x] **T-031** Order Status Tracking Widget (M4). Built an App Block specifically scoped to the `customers/order` Shopify template. Pre-fills the logged-in user's phone number and dynamically injects the order ID so customers can easily opt-in to WhatsApp shipping notifications for a specific purchase.
- [x] **T-030** "Opt-in for WhatsApp Updates" Checkbox (M4). Built a Shopify Theme App Extension Block (`target: "section"`) for WhatsApp Lead Capture. Merchants can drop this form anywhere (Product page, Cart, Homepage) to collect phone numbers before checkout. Features fully customizable text, colors, and layout squircle properties.
- [x] **T-029** Floating WhatsApp Chat Button (M4). Created a Shopify Theme App Extension (`extensions/tellero-chat`) with an App Embed block for a squircle floating chat widget. Added deep linking UI in Tellero dashboard for merchants to configure and enable it directly in their Shopify Theme Editor.
- [x] **T-028** Fix Shopify OAuth HMAC validation and reconnect routing (Ad-hoc). Fixed a compiler error in the new install flow caused by an unrenamed `userId` variable. Corrected the HMAC calculation in `src/lib/shopify/shopifyAuth.ts` to include the `session` parameter, aligning with Shopify's latest OAuth signature implementation which previously caused valid app installs to fail. Appended debug tracking parameters (`?debug=reconnected`) to the magic link callback so merchants can visibly verify the auth flow result in their browser. Renamed the `/dashboard/broadcast` directory to `/dashboard/broadcasts` across the entire codebase to fix a typo causing a 404 on the Shopify reconnect redirect.
- [x] **T-026** Broaden agent access to operational areas (Ad-hoc, product decision). Agents now get full view + actions on Contacts, Templates, and Broadcasts (in addition to Inbox + Profile). Added those prefixes to `canAccessPath` in `src/lib/access.ts` (tests updated). Removed the agent API blocks added in T-025 from broadcasts/send and all templates routes (create, [id] PUT/DELETE, [id]/submit, sync, submit-all-drafts). Added migration `20260616_agent_write_contacts_tags.sql` switching contacts + tags write policies from `is_org_admin` to `is_org_member` so agents can edit/tag from the client (templates already member-writable; broadcasts send via service role). Agents still excluded from billing, team, automations, analytics, integrations, brand settings.
- [x] **T-025** Role-based team-member access across the dashboard (Ad-hoc). Added `src/lib/access.ts` (`canAccessPath`/`landingPathFor`, TDD) encoding the access model from the Team settings page: owner = everything incl billing, admin = everything except billing, agent = inbox + own profile only. Extended `OrgProvider` to expose `role`; added `RouteGuard` (in dashboard layout) that redirects members away from pages they can't open and holds render until role is known so forbidden pages never flash or fire blocked fetches. Filtered the `Sidebar` nav by role (agents see only Inbox + Profile; admins lose Billing; plan badge no longer links to billing for non-owners). Tightened API to match: billing create-subscription routes are now owner-only (were admin-allowed); templates create/update/delete/submit/sync/submit-all-drafts now block agents. Reviewed broadcast page end-to-end (post T-022): no bugs found.
- [x] **T-024** Repair SECURITY DEFINER functions broken by the `user_id` -> `org_id` rename (Planned). Added `supabase/migrations/20260616_fix_rename_security_definer_functions.sql`. Function bodies are stored as text and aren't rewritten on column rename, so `attribute_order_revenue` + `mark_checkout_recovered` still queried the gone `user_id` column (on `analytics_events`, `automation_queue`, `broadcasts`, `abandoned_checkouts`) and errored at runtime. Recreated both using `org_id`; renamed the param to `p_org_id` to match the call sites in `orders-create/route.ts` (which already passed `p_org_id`). Updated `src/types/database.ts` (`mark_checkout_recovered` param + added missing `attribute_order_revenue` signature).
- [x] **T-023** Fix RLS policies referencing `auth.uid() = org_id` after the `user_id` -> `org_id` rename (Planned). Added `supabase/migrations/20260616_fix_rls_org_member_policies.sql`: drops every legacy owner-only policy on each tenant table and recreates a consistent set via the existing `is_org_member`/`is_org_admin` SECURITY DEFINER helpers. Owners locked agents/admins out because their `auth.uid()` never equals the org id. New model: every member can READ org data; admins/owners manage records (insert/update/delete); inbox tables (conversations, messages, contact_tags) + templates are fully operable by all members; billing is owner/admin only. Also repaired `set_contact_attribute`/`delete_contact_attribute` whose bodies still referenced the renamed `contacts.user_id`. Tables fully broken for non-owners before the fix: `automations`, `broadcast_recipients`, `shopify_stores`, `billing_events`. Discovered `attribute_order_revenue` + `mark_checkout_recovered` are also broken by the rename -> filed T-024.
- [x] **T-022** Broadcast page edge cases and bugs (Ad-hoc). Fixed timezone bug when scheduling broadcasts (local time was incorrectly appended with 'Z'). Added 5-minute schedule boundary validation. Prevented DetailModal from retaining stale broadcast status by synchronizing with the parent list's data updates. Improved auto-polling on the list page to also poll for scheduled broadcasts coming due. Fixed a MessagePreview regex bug to support variable numbers > 1. Corrected `useCampaignAnalytics.ts` mapping `failed` to 0 instead of inaccurately using `opted_out`.

## 2026-06-15
- [x] **T-021** Fix `user_id` to `org_id` fallout on broadcast analytics & UI (Ad-hoc). Narrowed down migration script to only touch tables with `user_id`. Fixed 403 error on `/api/broadcasts/[id]/analytics/route.ts` where `broadcast.user_id` was checked instead of `org_id`. Hardened the analytics `DetailModal` to not crash on fetch failures. Added real-time tracking of `actual_sent` counting against `broadcast_recipients` so funnel accurately shows active sents vs initial intended recipients. Added 10s polling and manual refresh to `DetailModal`. Renamed campaigns Analytics page title.

- [x] **T-020** Migrate manual localStorage caching to SWR (Backlog). Replaced all raw `useState` and `useEffect` manual `localStorage` hooks across the 7 automation settings pages with `useSWR` using a custom global `localStorageProvider` in a new `SWRProvider.tsx`. This keeps offline/instant loads while preventing code bloat.

- [x] **T-019** Shopify setup polish (M3). Added a post-connection checklist to guide users to automations after Shopify OAuth. Enhanced the Webhook Status panel to show the exact active webhook topics. Improved manual sync feedback with specific counts for customers and orders synced, and added a warning flag if webhook activity hasn't been seen in 7 days. (Features)

- [x] **T-018** Shopify refunds webhook (M3). Added the `refunds/create` webhook to Shopify integration, allowing Tellero to correctly decrement `contacts.total_spent` when an order is refunded, ensuring accurate segment filtering for VIPs based on total spend over time. Logged the decision to skip `orders/paid` as card/UPI payments are typically instant for Shopify merchants. (Features)

- [x] **T-017** Product picker for broadcasts and automations (M3). Built a reusable `ProductPicker` component that lazily fetches live product data from Shopify. Integrated this picker into both the broadcast composer (`page.tsx`) and the automations UI (`AutomationSlotMapper.tsx`) to auto-fill product titles, generated Shopify URLs, and prices for template variables containing "product" / "url" / "price". (Features)


- [x] **T-016** Welcome flow automation (M2). Added a new `welcome` event type and `welcome_settings` JSONB column to organizations. Updated `orders-create` webhook to trigger a welcome automation on the customer's very first order with a configurable delay. Built the `/dashboard/automations/welcome` UI to toggle and manage this flow, mapping variables like `customer_name` and `brand_name`. (Features)

- [x] **T-015** Broadcast analytics detail view (M1). Built a slide-over UI that appears when clicking a broadcast row. Implemented `/api/broadcasts/[id]/analytics` route to fetch delivery funnel stats and attribute revenue based on `analytics_events`. Adjusted Shopify order attribution window to 24 hours. (Features)
- [x] **T-014** Replace board.md with docs/todo.md - simplified task system: flexible queue format, ad-hoc work goes straight to done.md, spec docs optional. Updated CLAUDE.md, GEMINI.md, deleted board.md. (Process)



- [x] **T-013** Expand unit test coverage for Shiprocket client library (`src/lib/shiprocket/ShiprocketClient.ts`), WhatsApp settings API endpoint (`src/app/api/settings/whatsapp/route.ts`), and Embedded Signup API endpoint (`src/app/api/settings/whatsapp/embedded-signup/route.ts`). Added tests for rate limiting, credentials validation, database updates, token exchange, and app subscriptions. (Testing)
- [x] **T-012** Implement comprehensive test coverage updates across key revenue and automation features (M1, M2): expanded testing for segment builder/resolver (`src/lib/segments.ts`), broadcast send API (`src/app/api/broadcasts/send/route.ts`), process-automations cron (`src/app/api/cron/process-automations/route.ts`), and scheduled broadcasts cron (`src/app/api/cron/process-scheduled-broadcasts/route.ts`). Added test scenarios covering database insertion errors, API failures, marketing opt-outs, reorder validation checks, Resend integration, and template variable fallback resolution. (Testing)
- [x] **T-011** Centralize the segment query builder into `src/lib/segments.ts` (tags, not_tag, numeric/date, attributes, AND/OR), now shared by the Segments UI (thin re-export), broadcasts, and the winback/review-request crons; replaced the weak inline `resolveSegment` in the broadcast send route. Added scheduled-broadcast processing: `segment_ref` column, `/api/cron/process-scheduled-broadcasts` (claims due `scheduled` broadcasts, resolves the segment, queues recipients with variables pre-resolved, flips to `sending`), pg_cron schedule, and shared `src/lib/broadcasts.ts` variable-resolution helpers. Tests: segment helpers, variable resolution, and the scheduling route happy/empty/failed paths (M1)
- [x] **T-006** Implement comprehensive test suite for `process-automations` route, covering COD timeouts, WhatsApp template sending, and reorder reminder logic (Testing)
- [x] **T-007** Implement test suite for `orders-create` Shopify webhook, verifying HMAC validation, contact/order saving, and automation queuing (Testing)
- [x] **T-008** Implement test suite for `broadcasts/send` API, verifying segment resolution, RBAC, and recipient pre-resolution (Testing)
- [x] **T-009** Implement test suite for `usePlan` hook, ensuring plan limits and feature flagging are correctly enforced (Testing)
- [x] **T-010** Implement test suite for `shopifyWebhooks` utility library, covering registration, deletion, and signature verification (Testing)
- [x] **T-003** Centralize the WhatsApp Cloud API send path into `src/lib/whatsapp/client.ts` (text + template + `resolvePhoneNumberId` that prefers the stored `phone_number_id` over a live lookup), migrating all 6 duplicated call sites: inbox reply/new, meta webhook, process-broadcasts, process-automations, BotService (Integrations)
- [x] **T-004** WhatsApp Embedded Signup: `meta_business_id` / `phone_number_id` / `meta_token_expires_at` columns, `embeddedSignup` lib (code exchange + auto `subscribed_apps`), `/api/settings/whatsapp/embedded-signup` route, and Facebook Login for Business button in WhatsAppSetup with manual form kept as fallback (Integrations)
- [x] **T-005** WhatsApp connection health: `validateWhatsAppToken` (classifies definitive auth failures vs transient), on-demand validation in the settings GET (`tokenValid`), and a "Reconnect needed" status state in WhatsAppSetup that surfaces the reconnect / Embedded Signup options when a stored token has gone invalid (Integrations)

## 2026-06-14

- [x] **T-001** Move WhatsApp from a standalone settings page into the Integrations page as a card + modal, reusing the existing setup UI (Integrations)
- [x] **T-002** Scope the checkpoint rule to risky work (CLAUDE.md, GEMINI.md, tdd-workflow skill) (Process)

- [x] **T-023** Flatten UI design: shifted to #FDFDFC off-white, replaced all button/input radii with pill shapes, and removed all drop-shadows globally.
- [x] **T-083** Extended the COD Confirmation automation to optionally convert COD orders to Prepaid. Implemented toggles and inputs in `CODSettingsCard.tsx` to enable dynamic flat discounts. Extended `shopifyClient.ts` to natively create Shopify Draft Orders containing a custom discount and pass the `invoice_url` natively into the `cod_confirmation` WhatsApp template (`checkout_url` variable). Built intelligent conversion tracking in `orders-create/route.ts` which safely detects prepaid conversions via Shopify tags, retroactively cancels the original COD order, and updates database records seamlessly.
